import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';
import './styles.css'; // Tambahkan file CSS untuk styling

ReactDOM.render(<App />, document.getElementById('root'));
